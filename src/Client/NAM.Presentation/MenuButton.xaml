<UserControl x:Class="NAM.Presentation.MenuButton"
             x:Name="window"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NAM.Presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Button Margin="0" HorizontalContentAlignment="Stretch" Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Source="Assets/Settings-512.png" Height="25" />
            <TextBlock Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,0"
                       VerticalAlignment="Center" Text="{Binding ElementName=window, Path=Text}" />
        </Grid>
    </Button>
</UserControl>
